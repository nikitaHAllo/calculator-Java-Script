1; // ================ Operators
let a = 10;
let b = 5;
let c = 32;
// c = c + a
// c = c - a
// c = c * a
// c =  c / a
// c += a
// c -= a
// c *= a
// c /= a функции сложения умножения деление
// c = 2 ** 2; возведение в квадрат

// =========== Data Types
const age = 29; //number
const name = 'Nikita'; // string
const isProgrammer = true; // boolean
let x; // undefined
// null
// console.log(typeof null);
// console.log(typeof (1 / x))
// console.log(typeof NaN)
//================ Priority
const fullAge = 17;
const birthYear = 2007;
const currentYear = 2024;
// > <
const isFullAge = currentYear - birthYear >
fullAge ;
const num1 = 42
const num2 = '42'
console.log(num1 === num2)